<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="teotw.com.mywidgets">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:appComponentFactory=""
        tools:replace="android:appComponentFactory">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.UpDownTextViewActivity" />
        <activity android:name=".activities.CreateViewInSubThreadActivity" />
        <activity android:name=".activities.ArcProgressActivity" />
        <activity android:name=".activities.StickyActivity" />
        <activity android:name=".activities.ShineButtonActivity" />
        <activity android:name=".activities.TagActivity" />
    </application>

</manifest>